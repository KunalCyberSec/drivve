import React from 'react';
import { View, Text } from 'react-native';

const RideScreen = () => {
  return (
    <View style={{ flex: 1, padding: 20 }}>
      <Text>Ride Screen — implement ride form here</Text>
    </View>
  );
};

export default RideScreen;
